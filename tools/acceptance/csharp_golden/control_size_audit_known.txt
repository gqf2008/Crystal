# 已知待核：写死尺寸 != 美术原生尺寸（**待办队列**，不是「白名单通过」）
# 生成：py -3.12 control_size_audit.py --repo . --data Data
# 每行 file<TAB>lib<TAB>index<TAB>w<TAB>h；清单内只提示，**新增**命中才 FAIL
# 背景：2026-09-26 发现本工具对 if let (Some(n),Some(h),Some(pr)) = (load...,load...,load...)
#       这种主流写法完全失明（元组里没有 let x = 绑定）=> 一度报 0 命中；补上元组解析与
#       normal 帧优先配对后浮现出下面这些，逐条核完就删掉对应行。
# 2026-09-26：首轮队列已全部核完并清零（同一 PR 内改尺寸 + 删表行）：
#   game_shop Prguse[361] 16x15（viewer 关闭）、Prguse2[240]/[243] 16x16（viewer 转身，两处各 2 个）
#   group Prguse[114] 28x25 / Title[133] 60x25 / Title[136] 60x25
#   mentor Prguse[114] 28x25 / Title[213] 76x25 / Title[216] 76x25
#   quest_log Title[193] 68x25 / Title[270] 68x25（两处）/ Title[273] 68x25
#   storage Title[210] 76x25（两处，MirMessageBox NoButton）
#   keyboard_layout Title[120] 60x25（C# 显式 Size 被 AutoSize 顶掉）
#   npc_goods Title[312] 80x25
# 判定口径：C# `MirButton : MirImageControl` 构造器置 `_autoSize = true`
# （`Client/MirControls/MirImageControl.cs:165-171`），`Size` getter 在 AutoSize 时直接返回
# `Library.GetTrueSize(Index)`（同文件 `:142-151`），`DrawControl` 只按 `DisplayLocation`
# 原尺寸绘制、不按 Size 拉伸（同文件 `:173-193`）⇒ 显式 Size 与写死尺寸都无效，
# 一律以美术原生（.Lib 图头）为准。
#
# 用法：本表是「已知待核」队列，不是白名单。命中不在表内 → FAIL（exit 1）。
