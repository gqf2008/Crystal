use crate::{compat::Coord, game_world::GameWorld, systems::RenderSystem};
use crate::compat::{GameResult, GraphicsContext, Canvas};

#[derive(ecs_macros::RenderSystem)]
pub struct UIRenderSystem;

impl RenderSystem for UIRenderSystem {
    fn draw(&mut self, _world: &GameWorld) -> GameResult {
        // TODO: macroquad UI 渲染
        // 使用 macroquad::ui 或直接绘制
        Ok(())
    }
}