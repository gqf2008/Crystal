# Client-Macroquad P3 回归执行记录

## 2026-02-13（基线记录）

- 提交哈希：`92df486`
- 执行范围：P3 清单建立后的首次基线登记（流程就绪性检查）
- 通过项：
  - 已形成《24.P3回归与性能检查清单》可执行模板
  - 已完成 CI 运行状态检查（`marcoquad-native-ui` 无相关 run；当前分支最近完成 run 无 failed jobs）
- 未通过项：
  - 暂未执行全量功能回归（等待后续 P1/P2 功能点落地后按清单逐项打勾）
- 阻塞项（环境/依赖）：
  - Linux 环境缺少 `wayland-client` 系统库时，`cargo test` 在依赖构建阶段失败
- 下次回归重点：
  - 结合 Inventory/Character 下一步移植结果，执行“场景链路 + 常用对话框开关”子集回归

## 2026-02-13（P1-Inventory 现状复核）

- 提交哈希：`323935e`
- 执行范围：按《26.P1核心交互实施清单》对 Inventory 现有实现做代码级复核
- 通过项：
  - `I1` 物品图标渲染已存在（`draw_slots` 中按 `icon_index` 绘制）
  - `I2` 物品数量显示已存在（`draw_item_count` / 拖拽态数量叠加文本）
  - `I3` 拖拽基础流已存在（`Drag::Dragging / Dropped`）
  - `I4` 格子交换已存在（`DragCommand::Swap` + `swap(from, to)`）
- 未通过项：
  - `I5` 背包与 Belt 最小互操作尚未接入
- 阻塞项（环境/依赖）：
  - UI 行为的运行时截图验证仍受本地图形环境与系统库约束（`wayland-client`）
- 下次回归重点：
  - 进入 `I5`：设计并接入 Inventory 与 Belt 的最小数据交换路径

## 2026-02-13（P1-I5 设计基线）

- 提交哈希：`96739ae`
- 执行范围：完成 I5 的最小互操作设计固化（实现前基线）
- 通过项：
  - 已落库《27.P1-I5背包快捷栏互操作设计》
  - 明确了放入/回收双向路径与失败回滚策略（避免物品丢失）
- 未通过项：
  - 尚未进入代码落地与运行时行为验证
- 阻塞项（环境/依赖）：
  - 运行时 UI 验证仍依赖图形环境与 `wayland-client` 系统库
- 下次回归重点：
  - 进入 I5 实现后，按 24 号清单执行“背包/快捷栏”交互回归并记录结果
