# Client-Macroquad P1-I5 实现任务清单（代码落地）

日期：2026-02-13

> 基于《27.P1-I5背包快捷栏互操作设计》，本清单用于直接指导下一步代码改动。

## 1. 目标

将 I5 从设计文档推进到“最小可运行代码闭环”。

## 2. 代码改动点（最小范围）

### 2.1 InventoryDialogHybrid

文件：`src/scenes/dialogs/game/inventory_dialog.rs`

- [x] 增加“发送到快捷栏”一次性请求状态字段
- [x] 增加请求读取接口（take 模式）
- [x] 增加取出物品与回滚接口（按槽位）
- [x] 增加从快捷栏插入物品接口（先叠加，再空位）

### 2.2 BeltDialogHybrid

文件：`src/scenes/dialogs/game/belt_dialog.rs`

- [x] 增加“发送到背包”一次性请求状态字段
- [x] 增加请求读取接口（take 模式）
- [x] 增加取出物品与回滚接口（按槽位）
- [x] 增加从背包插入物品接口（先叠加，再空位）

### 2.3 MainDialog（协调层）

文件：`src/scenes/dialogs/game/main_dialog.rs`

- [x] 在统一绘制/更新流程末尾增加 interop 协调函数
- [x] 消费 Inventory/Belt 请求并做双向交换
- [x] 失败时执行回滚，确保无物品丢失

## 3. 建议交互触发（临时）

为降低改动风险，I5 首版可采用“右键快捷转移”作为触发：

- 背包格右键：请求转移到快捷栏
- 快捷栏格右键：请求回收到背包

后续再演进为更完整拖拽跨窗体验。

## 4. 验收清单（实现完成即勾）

- [ ] 背包 -> 快捷栏：成功放入
- [ ] 快捷栏 -> 背包：成功回收
- [ ] 快捷栏满时：转移失败且物品回滚
- [ ] 背包满时：回收失败且物品回滚
- [ ] 不影响现有双击使用与同窗拖拽交换

## 5. 回归记录要求

- 实现后按《24.P3回归与性能检查清单》执行相关子集
- 在《25.P3回归执行记录》追加 I5 落地执行结果
