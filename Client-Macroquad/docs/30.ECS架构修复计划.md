# ECS 架构修复计划

> 关联文档: [29.ECS系统架构审计报告.md](29.ECS系统架构审计报告.md)  
> 创建日期: 2026-02-17

---

## 修复状态总览

| 编号 | 严重级别 | 问题 | 状态 | 修复文件 |
|------|---------|------|------|---------|
| F-01 | P0 | ENTITY_RENDER/EFFECT_RENDER 优先级冲突 | ✅ 完成 | systems/mod.rs (×2) |
| F-02 | P0 | FloatingTextSystem 复用 PARTICLE 优先级 | ✅ 完成 | systems/mod.rs, game_scene.rs |
| F-03 | P0 | map_viewer.rs unsafe static mut | ✅ 完成 | bin/map_viewer.rs |
| F-04 | P1 | Health.heal() 负数金额漏洞 | ✅ 完成 | components/combat.rs (×2) |
| F-05 | P1 | Mana.restore() 负数金额漏洞 | ✅ 完成 | components/combat.rs (×2) |
| F-06 | P1 | Mana.consume() 负数消耗漏洞 | ✅ 完成 | components/combat.rs (×2) |
| F-07 | P2 | 网络反序列化范围校验 | ✅ 完成 | network_apply_system.rs |
| F-08 | P2 | Position NaN/Infinity 校验 | ✅ 完成 | components/core.rs |
| F-09 | P2 | 优先级唯一性运行时检查 | ✅ 完成 | systems/mod.rs |
| F-10 | P2 | RefCell 嵌套借用保护 | ✅ 完成 | resources/libraries.rs |
| F-11 | P2 | 派生宏 trait bound 验证 | ✅ 完成 | ecs_macros/src/lib.rs |
| F-12 | P3 | 备份文件清理 | ✅ 完成 | .gitignore |
| F-13 | P3 | unwrap() panic 风险 | ✅ 完成 | sprite_system.rs, character.rs, effect_system.rs, debug_system.rs |
| F-14 | P3 | TODO 功能实现 | 📋 持续 | 多个系统文件 |

---

## 已完成修复详情

### F-01: 渲染优先级冲突修复

```diff
- pub const EFFECT_RENDER: u32 = 920;
+ pub const EFFECT_RENDER: u32 = 925;
```

**验证方式**: 确认 EffectRenderSystem 在 EntityRenderSystem 之后执行。

### F-02: FloatingTextSystem 优先级修复

```diff
  // systems/mod.rs - 新增常量
+ pub const FLOATING_TEXT: u32 = 615;

  // game_scene.rs - 使用正确优先级
- priority::PARTICLE,
+ priority::FLOATING_TEXT,
```

### F-03: unsafe 消除

```diff
- static mut FIRST_RENDER: bool = true;
+ use std::sync::atomic::{AtomicBool, Ordering};
+ static FIRST_RENDER: AtomicBool = AtomicBool::new(true);

- unsafe {
-     if FIRST_RENDER {
-         FIRST_RENDER = false;
-     }
- }
+ if FIRST_RENDER.load(Ordering::Relaxed) {
+     FIRST_RENDER.store(false, Ordering::Relaxed);
+ }
```

### F-04/F-05/F-06: Health/Mana 输入校验

```diff
  pub fn heal(&mut self, amount: i32) {
+     if amount <= 0 { return; }
-     self.current = (self.current + amount).min(self.max);
+     self.current = (self.current.saturating_add(amount)).min(self.max);
  }

  pub fn consume(&mut self, cost: i32) -> bool {
+     if cost <= 0 { return false; }
      ...
  }

  pub fn restore(&mut self, amount: i32) {
+     if amount <= 0 { return; }
-     self.current = (self.current + amount).min(self.max);
+     self.current = (self.current.saturating_add(amount)).min(self.max);
  }
```

---

## 待实施项指南

### F-07: 网络反序列化校验 (P2)

在 `network_apply_system.rs` 的 HealthChanged/ManaChanged 处理中添加:

```rust
hp.current = (cur as i32).clamp(0, hp.max);
mp.current = (cur as i32).clamp(0, mp.max);
```

### F-08: Position NaN 校验 (P2)

```rust
pub fn new(x: f32, y: f32) -> Self {
    debug_assert!(!x.is_nan() && !x.is_infinite(), "Position x is invalid");
    debug_assert!(!y.is_nan() && !y.is_infinite(), "Position y is invalid");
    Self { x, y }
}
```

### F-09: 优先级唯一性检查 (P2)

在 `SystemScheduler::add_system()` 中添加:

```rust
#[cfg(debug_assertions)]
{
    let existing = self.systems.iter().any(|e| e.priority() == priority);
    if existing {
        tracing::warn!("Priority {} already used by another system!", priority);
    }
}
```

### F-12: 备份文件清理 (P3)

```bash
# 在 .gitignore 添加
*.bak*

# 删除已存在的备份文件
git rm --cached Client-Macroquad/src/systems/rendering/sprite_system/*.bak*
```
