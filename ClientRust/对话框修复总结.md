# 对话框修复总结 - 2025-10-17

## ✅ 已修复问题

### 1. **修改密码对话框缺纹理** - 已修复
**问题**: 使用了错误的纹理索引 64
**修复**: 改为正确的索引 **50** (来自 C# 原版)
```rust
// 错误: let dialog_bg = mlibrary_assets.get_texture("Prguse", 64, images);
// 正确:
let dialog_bg = mlibrary_assets.get_texture("Prguse", 50, images);
```

### 2. **修改密码对话框按钮位置偏上** - 已修复
**问题**: 按钮位置使用了自动计算,不符合 C# 原版
**修复**: 使用 C# 原版的精确坐标

**OK 按钮修正:**
```rust
// 错误: left: 135.0, top: dialog_height - 40.0
// 正确:
left: Val::Px(80.0),   // C# Location = new Point(80, 236)
top: Val::Px(236.0),
```

**Cancel 按钮修正:**
```rust
// 错误: left: 409.0, top: dialog_height - 40.0
// 正确:
left: Val::Px(222.0),  // C# Location = new Point(222, 236)
top: Val::Px(236.0),
```

### 3. **修改密码对话框按钮纹理索引错误** - 已修复
**问题**: 使用了新账号对话框的按钮纹理索引 (200-205)
**修复**: 改为修改密码对话框专用的按钮纹理索引 (107-112)

**按钮纹理修正:**
```rust
// OK Button
ButtonTextures {
    normal_index: 107,   // 错误: 200
    hover_index: 108,    // 错误: 201
    pressed_index: 109,  // 错误: 202
}

// Cancel Button
ButtonTextures {
    normal_index: 110,   // 错误: 203
    hover_index: 111,    // 错误: 204
    pressed_index: 112,  // 错误: 205
}
```

## 📊 对话框纹理索引参考表

| 对话框类型 | 背景纹理库 | 背景索引 | OK按钮索引 | Cancel按钮索引 |
|-----------|-----------|---------|-----------|--------------|
| **新账号** | Prguse | 63 | 200/201/202 | 203/204/205 |
| **修改密码** | Prguse | 50 | 107/108/109 | 110/111/112 |

按钮索引格式: normal/hover/pressed

## ✅ 验证通过的功能

1. **对话框背景显示** - ✅ 正常
   - 新账号对话框背景完整显示
   - 修改密码对话框背景完整显示
   - 对话框自动居中

2. **按钮悬停效果** - ✅ 正常
   - 日志显示: `🖱️ Button hover: ON (index 204)`
   - 纹理正确切换

3. **按钮按下效果** - ✅ 正常
   - 日志显示: `🖱️ Button pressed (index 205)`
   - 纹理正确切换

4. **按钮功能** - ✅ 正常
   - Cancel 按钮正常关闭对话框
   - OK 按钮正常关闭对话框 (未来会添加验证)

## ❌ 待实现功能

### 1. **输入框系统** (高优先级)
当前对话框只有按钮,没有输入框。需要添加:

**新账号对话框输入框:**
```
位置格式: (x, y, width, height)

AccountID:          (226, 103, 136, 18)  - 必填
Password1:          (226, 129, 136, 18)  - 必填
Password2:          (226, 155, 136, 18)  - 必填
UserName:           (226, 189, 136, 18)  - 可选
BirthDate:          (226, 215, 136, 18)  - 可选
Question:           (226, 250, 190, 18)  - 可选 (注意宽度190)
Answer:             (226, 276, 190, 18)  - 可选 (注意宽度190)
Email:              (226, 311, 136, 18)  - 可选
```

**修改密码对话框输入框:**
```
AccountID:          (178, 75, 136, 18)   - 必填
CurrentPassword:    (178, 113, 136, 18)  - 必填
NewPassword1:       (178, 151, 136, 18)  - 必填
NewPassword2:       (178, 188, 136, 18)  - 必填
```

### 2. **输入框组件需求**
参考原版 Rust 实现 (`src/scenes/login_scene.rs`):

```rust
// 输入框边框颜色根据验证状态变化:
- 灰色 (128, 128, 128): 未验证
- 绿色 (0, 255, 0):    有效
- 红色 (255, 0, 0):    无效

// 输入框文本:
- 字体: "AlibabaPuHuiTi"
- 字号: 21.0
- 密码字段显示: "*".repeat(password.len())
- 文本超长时从右往左截取

// 边框:
- 绘制方式: Mesh::new_rectangle with DrawMode::stroke(1.0)
- 尺寸: Rect::new(x, y, 136.0, 18.0)
```

### 3. **输入验证逻辑**
需要实现 C# 原版的验证规则:

```rust
// AccountID 验证
Regex: r"^[A-Za-z0-9]{3,15}$"
Length: 3-15 characters
Chars: a-z A-Z 0-9

// Password 验证
Regex: r"^[A-Za-z0-9]{5,15}$"
Length: 5-15 characters
Chars: a-z A-Z 0-9

// Password2 验证
Must match Password1

// Email 验证 (可选)
Regex: r"\w+([-+.]\w+)*@\w+([-.]\w+)*\.\w+([-.]\w+)*"
Max length: 50

// UserName (可选)
Max length: 20

// BirthDate (可选)
Format: DateTime
Max length: 10

// Question/Answer (可选)
Max length: 30
```

### 4. **Description 标签框**
C# 原版位置: `(15, 340, 300, 70)`
- 初始隐藏 (`Visible = false`)
- 当输入框获得焦点时显示对应说明
- 不同输入框有不同的说明文本

### 5. **焦点管理**
- Tab 键在输入框之间切换焦点
- Enter 键提交表单
- Esc 键关闭对话框
- 鼠标点击切换焦点

### 6. **光标显示**
- 闪烁动画 (每0.5秒切换显示/隐藏)
- 只在获得焦点的输入框显示

### 7. **网络通信**
```rust
// 新账号
Network.Enqueue(new C.NewAccount {
    AccountID,
    Password,
    EMailAddress,
    BirthDate,
    UserName,
    SecretQuestion,
    SecretAnswer,
});

// 修改密码
Network.Enqueue(new C.ChangePassword {
    AccountID,
    CurrentPassword,
    NewPassword,
});
```

## 📝 实现建议

### 方案A: 复用现有输入框组件
登录界面已经有 `AccountIdInput` 和 `PasswordInput` 组件,可以复用:

**优点:**
- 代码复用
- 已经有光标闪烁动画
- 已经有输入处理逻辑

**缺点:**
- 需要扩展支持多个输入框
- 需要添加验证状态显示

### 方案B: 参考原版 Rust 实现
`src/scenes/login_scene.rs` 中的 `draw_new_account_dialog` 和 `draw_change_password_dialog` 有完整实现:

**优点:**
- 完整的功能参考
- 验证逻辑已实现
- 边框颜色反馈已实现

**缺点:**
- 使用 ggez 绘制,需要转换为 Bevy UI
- 代码量较大

### 推荐方案: 混合方案
1. 创建新的 `DialogInputField` 组件
2. 复用登录界面的输入处理系统 (`handle_text_input`)
3. 添加验证状态显示 (边框颜色)
4. 参考原版的验证逻辑

## 🎯 下一步计划

### 第一阶段: 基础输入框
1. 创建 `DialogInputField` 组件
2. 在新账号对话框添加 AccountID 和 Password 输入框
3. 实现基础输入和光标显示

### 第二阶段: 完整输入系统
1. 添加所有8个输入框 (新账号)
2. 添加所有4个输入框 (修改密码)
3. 实现焦点切换 (Tab/鼠标)

### 第三阶段: 验证和反馈
1. 实现所有验证规则
2. 边框颜色反馈
3. OK 按钮启用/禁用逻辑

### 第四阶段: 网络通信
1. 表单提交
2. 网络包发送
3. 服务器响应处理

---

## 🔍 调试日志参考

成功的日志示例:
```
📝 Creating New Account Dialog
📐 Dialog size: 600x470
✅ New Account Dialog created
🖱️ Button hover: ON (index 204)
🖱️ Button pressed (index 205)
❌ Dialog Cancel clicked
🚪 All dialogs closed
```

修改密码对话框:
```
🔑 Creating Change Password Dialog
📐 Dialog size: 360x310
✅ Change Password Dialog created
🖱️ Button hover: ON (index 111)
🖱️ Button pressed (index 112)
❌ Dialog Cancel clicked
🚪 All dialogs closed
```

---

## 总结

✅ **已完成**: 对话框背景、按钮位置、按钮纹理、按钮功能
❌ **待实现**: 输入框、验证、光标、焦点管理、网络通信

当前对话框已经是**可视化正常、可交互的框架**,但还是**"死的"**因为没有输入功能。下一步重点是实现输入框系统。
