fn main() {fn main() {

    // 占位二进制，旧的 map_viewer_v2 已移除或迁移；    eprintln!("  map_viewer_v2 暂时禁用 - 依赖已删除的组件");

    // 保留此占位文件以便 cargo 构建/检查不会失败。    eprintln!("请使用主程序: cargo run --bin mir2x");

    println!("map_viewer_v2 stub - disabled");    std::process::exit(1);

}}

